## commands auto generated  - please adjust! ###
## This repository is relative the ${name} model


from src.repositories.sql_repository import SqlRepository

class ${model_name}Repositoriy(SqlRepository):
    model = ${model_name}
